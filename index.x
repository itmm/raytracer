# Raytracer Challenge

```
@Def(| ./cc.sh)
	@put(includes);
	@put(types);
	@put(functions);
	int main(int argc, char **argv) {
		@put(main);
	}
@End(| ./cc.sh)
```

```
@def(main)
	@put(unit-tests);
@end(main)
```

```
@inc(tupels.x)
```

```
@inc(colors.x)
```

```
@inc(ppm.x)
```

```
@inc(matrices.x)
```

```
@inc(transforms.x)
```

```
@inc(rays.x)
```

```
@inc(light.x)
```

```
@inc(scene.x)
```

```
@inc(shadows.x)
```


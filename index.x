# Raytracer Challenge

```
@Def(| ./cc.sh)
	@put(includes);
	@put(types);
	@put(functions);
	int main(int argc, char **argv) {
		@put(main);
	}
@End(| ./cc.sh)
```

```
@def(main)
	@put(unit-tests);
@end(main)
```

```
@inc(points.x)
```

